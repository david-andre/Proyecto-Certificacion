@model IEnumerable<BEUCertificacion.DetallePedido>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Servicio.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Servicio.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iddetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.iddetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iddetalle })
        </td>
    </tr>
}

</table>
